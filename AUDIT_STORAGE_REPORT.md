# Аудит хранения данных проекта VIBES

**Дата:** 10 января 2026
**Цель:** Выявить данные, хранящиеся на клиенте вместо серверной БД

---

## Саммари

Обнаружено **11 типов данных**, хранящихся в localStorage. Из них:
- **5 критично важных** должны быть в базе данных
- **1 устаревший mock** требует полного удаления
- **3 локальных настроек** можно оставить (тема, звуки, кэш)
- **2 частично синхронизированных** (чат, AI-инструкция)

Главная проблема: **прогресс roadmap, история чата, onboarding** хранятся только локально и теряются при смене браузера.

---

## Таблица найденных проблем

| # | Данные | Где хранится | Файл/Компонент | Есть в БД? | Рекомендация |
|---|--------|--------------|----------------|------------|--------------|
| 1 | **vibes_roadmap_progress** | localStorage | Roadmaps.tsx:65 | ⚠️ Есть таблица, нет API | Создать POST/PUT endpoint |
| 2 | **vibes_chat_history** | localStorage | Assistant.tsx:176 | ✅ Есть таблица | Синхронизировать при каждом сообщении |
| 3 | **vibes_onboarded_{userId}** | localStorage | App.tsx:292 | ✅ Есть поле onboarding_completed | Использовать БД |
| 4 | **vibes_ai_system_instruction** | localStorage | Assistant.tsx:176, AdminAssistant.tsx:89 | ✅ Есть таблица ai_system_instructions | Сохранять активную в БД |
| 5 | **vibes_students_db** | localStorage | App.tsx:155 | ❌ Mock данные | ❌ **УДАЛИТЬ** (устарело) |
| 6 | **vibes_invites** | localStorage | App.tsx:151 | ✅ Есть таблица | Убрать кэш (есть API) |
| 7 | **theme** | localStorage | App.tsx:146 | ❌ | 🟢 Можно оставить локально |
| 8 | **vibes_sound_enabled** | localStorage | SoundContext.tsx:47 | ❌ | 🟢 Можно оставить локально |
| 9 | **vibes_sound_volume** | localStorage | SoundContext.tsx:56 | ❌ | 🟢 Можно оставить локально |
| 10 | **vibes_cache_*** | localStorage | lib/cache.ts | ❌ | 🟢 Кэш - можно оставить |
| 11 | **vibes_token** | localStorage | fetchWithAuth.ts:30 | ❌ | ✅ JWT - нормально хранить локально |

---

## Детальный отчёт по критичным проблемам

### 🔴 1. Прогресс дорожных карт (Roadmaps)

**Текущее хранение:** localStorage
**Ключ:** `vibes_roadmap_progress`
**Структура:** `{ roadmapId: [stepId1, stepId2, ...] }`
**Файлы:** `views/Roadmaps.tsx:53-66`

**Риски:**
- Потеря прогресса при смене браузера/устройства
- Прогресс не отображается в админке
- Невозможно аналитировать обучение студентов

**Рекомендация:**
Таблицы в БД уже есть:
- `user_roadmap_progress` - прогресс по картам
- `user_roadmap_step_progress` - прогресс по шагам

**Требуется:**
1. Создать API endpoint `POST /api/roadmaps/progress` для сохранения шага
2. Создать `DELETE /api/roadmaps/progress` для снятия галочки
3. Модифицировать `Roadmaps.tsx` - сохранять каждый toggleStep в БД

**API для создания:**
```typescript
// api/roadmaps.ts
async function updateProgress(req, res) {
  const { stepId, completed } = req.body;
  const userId = tokenData.userId;

  if (completed) {
    await query(
      `INSERT INTO user_roadmap_step_progress (user_id, step_id, completed_at)
       VALUES ($1, $2, NOW())
       ON CONFLICT (user_id, step_id) DO NOTHING`,
      [userId, stepId]
    );
  } else {
    await query(
      `DELETE FROM user_roadmap_step_progress
       WHERE user_id = $1 AND step_id = $2`,
      [userId, stepId]
    );
  }
}
```

---

### 🔴 2. История чата с AI

**Текущее хранение:** localStorage
**Ключ:** `vibes_chat_history`
**Структура:** `[{ id, role, text, timestamp }, ...]`
**Файлы:** `views/Assistant.tsx:148-178`

**Риски:**
- История теряется при смене браузера
- Админ не видит вопросы студентов
- Нет аналитики по популярным темам

**Рекомендация:**
Таблица `chat_messages` уже существует! Нужно только синхронизировать.

**Требуется:**
1. При отправке сообщения → POST в `chat_messages`
2. При загрузке чата → GET из `chat_messages` (последние 50)
3. При очистке чата → DELETE от пользователя

**API для создания:**
```typescript
// api/chat.ts добавить перед отправкой в AI
async function saveMessage(userId, role, text) {
  await query(
    `INSERT INTO chat_messages (user_id, role, content, model_used)
     VALUES ($1, $2, $3, 'google/gemini-2.5-flash-lite')`,
    [userId, role, text]
  );
}
```

**API для загрузки истории:**
```typescript
// GET /api/chat/history
async function getHistory(req, res) {
  const { rows } = await query(
    `SELECT id, role, content as text, created_at as timestamp
     FROM chat_messages
     WHERE user_id = $1
     ORDER BY created_at DESC LIMIT 50`,
    [tokenData.userId]
  );
  return rows.reverse(); // Старые сначала
}
```

---

### 🔴 3. Статус онбординга

**Текущее хранение:** localStorage
**Ключ:** `vibes_onboarded_${userId}`
**Значение:** `"true"`
**Файлы:** `App.tsx:250-293`

**Риски:**
- На одном браузере онбординг пройден, на другом - снова показывается
- Нет в админке - неизвестно, кто прошёл, кто нет

**Рекомендация:**
Использовать существующее поле `users.onboarding_completed`.

**Требуется:**
1. Убрать localStorage
2. При завершении онбординга → PUT `/api/user/onboarding`
3. При загрузке проверять `user.onboarding_completed`

**API для создания:**
```typescript
// PUT /api/user/onboarding
async function completeOnboarding(req, res) {
  await query(
    `UPDATE users SET onboarding_completed = true
     WHERE id = $1`,
    [tokenData.userId]
  );
  return successResponse({ success: true });
}
```

---

### 🔴 4. Системная инструкция AI

**Текущее хранение:** localStorage
**Ключ:** `vibes_ai_system_instruction`
**Файлы:** `Assistant.tsx:141-144`, `AdminAssistant.tsx:39-90`

**Риски:**
- Админ меняет инструкцию → студент видит старую
- Нет версионности
- Нельзя быстро откатить ошибку

**Рекомендация:**
Таблица `ai_system_instructions` существует! Использовать её.

**Текущая таблица:**
```sql
CREATE TABLE ai_system_instructions (
  id UUID PRIMARY KEY,
  name VARCHAR(100) NOT NULL,
  content TEXT NOT NULL,
  is_active BOOLEAN DEFAULT FALSE,
  created_by_id UUID REFERENCES users(id),
  created_at TIMESTAMPTZ
);
```

**Требуется:**
1. Админка → сохранять в `ai_system_instructions` с `is_active = true`
2. API → загружать активную инструкцию из БД
3. Убрать localStorage

---

### 🔴 5. Mock база студентов (УДАЛИТЬ)

**Текущее хранение:** localStorage
**Ключ:** `vibes_students_db`
**Файлы:** `App.tsx:35-38, 154-156`

**Проблема:**
Это устаревший mock! Студенты теперь из БД (`/api/admin?resource=students`), но localStorage всё равно используется в `AdminStudents.tsx`.

**Рекомендация:**
**Полностью удалить** использование `vibes_students_db`:
1. Убрать `initializeStudents()` (App.tsx:35-39)
2. Убрать `useEffect` с сохранением (App.tsx:154-156)
3. В `AdminStudents.tsx` загрузить из API вместо mock

---

### 🟡 6. Инвайты (кэш)

**Текущее хранение:** localStorage
**Ключ:** `vibes_invites`
**Файлы:** `App.tsx:28-33, 150-152`

**Проблема:**
Есть полноценный API (`/api/admin?resource=invites`), но инвайты всё равно кэшируются локально.

**Рекомендация:**
Убрать кэш - данные загружаются из API, локальное хранение избыточно.

---

## 🟢 Данные, которые можно оставить локально

| Данные | Причина |
|--------|---------|
| **theme** | Личная настройка UI, не критичная для синхронизации |
| **vibes_sound_enabled** | Звуки - локальная UX-настройка |
| **vibes_sound_volume** | Громкость - локальная UX-настройка |
| **vibes_cache_*** | Кэш статичных данных (стили, промпты) - это кэш, а не хранение |
| **vibes_token** | JWT - стандартная практика хранения токена в localStorage |

---

## Чек-лист проверки

- [x] Проверен весь фронтенд-код на использование Web Storage API
- [x] Проверены все localStorage ключи
- [x] Изучена полная схема базы данных
- [x] Проанализирован весь API (GET/POST/PUT/DELETE)
- [x] Изучен функционал админ-панели
- [x] Проверены все формы на сайте
- [x] Проверены пользовательские настройки
- [x] Проверен прогресс обучения (roadmaps, lessons)
- [x] Проверена авторизация и сессии

---

## План миграции по приоритету

### Приоритет 1 (Критично - 3-5 дней)
1. **Roadmap progress** - Создать API endpoints, модифицировать UI
2. **Chat history** - Синхронизировать с `chat_messages`
3. **Onboarding** - Использовать `users.onboarding_completed`
4. **AI system instruction** - Перенести в `ai_system_instructions`

### Приоритет 2 (Важно - 1-2 дня)
5. **Mock students DB** - Полностью удалить устаревший код
6. **Invites кэш** - Убрать из localStorage

### Приоритет 3 (Низкий)
7. **User settings** (тема, звуки) - можно оставить локально

---

## Оценка трудозатрат

| Задача | Сложность | Время |
|-------|-----------|-------|
| Roadmap progress API + UI sync | Средняя | 1-2 дня |
| Chat history API + sync | Средняя | 1-2 дня |
| Onboarding sync | Лёгкая | 2-4 часа |
| AI instruction sync | Лёгкая | 2-4 часа |
| Удалить mock students DB | Лёгкая | 2-4 часа |
| Убрать invites кэш | Лёгкая | 1-2 часа |
| **ИТОГО** | | **3-5 дней** |

---

## Статистика

- **Всего найдено:** 11 localStorage ключей
- **Критично:** 5 (roadmap, chat, onboarding, AI, mock DB)
- **Важно:** 1 (invites кэш)
- **Низкий приоритет:** 5 (токен, тема, звуки, кэш)
- **Устаревший код:** 1 (mock students DB)

---

## Дополнительные замечания

### Архитектурные рекомендации

1. **Единый подход к синхронизации:**
   - Создать хук `useSyncedState` для данных, требующих персистентности
   - Пример: `useSyncedState('roadmaps', '/api/roadmaps/progress')`

2. **Оптимистичные обновления:**
   - Обновлять UI сразу, отправлять запрос в фоне
   - При ошибке - откатывать UI и показывать notification

3. **Оффлайн-режим:**
   - Сохранять изменения в localStorage
   - При восстановлении соединения - синхронизировать с сервером

### Безопасность

- ✅ JWT токен в localStorage - допустимо для Vercel serverless
- ⚠️ Рассмотреть httpOnly cookies для улучшенной безопасности
- ✅ Все API endpoints требуют авторизацию

### Производительность

- ✅ Кэш статичных данных (стили, промпты) реализован корректно
- ⚠️ Roadmap progress можно кэшировать на короткое время (1 минута)
- ✅ Chat history загружает только последние 50 сообщений

---

## Ссылки на файлы проекта

- `database/schema.sql` - Схема БД (строки 499-526: roadmap progress, 616-634: chat messages)
- `views/Roadmaps.tsx` - Прогресс roadmap в localStorage
- `views/Assistant.tsx` - История чата в localStorage
- `App.tsx` - Onboarding, mock students, invites кэш
- `SoundContext.tsx` - Настройки звука (можно оставить локально)
- `lib/cache.ts` - Утилита кэширования
- `api/chat.ts` - Чат API (только отправка, нет сохранения истории)
- `api/content.ts` - Content API (roadmaps GET с прогрессом из БД, но нет POST/PUT)
- `api/admin.ts` - Admin API (студенты, инвайты - уже работают с БД)
