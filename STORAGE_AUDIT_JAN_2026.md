# Отчет по аудиту хранения данных (Январь 2026)

## Саммари
Проект находится в гибридном состоянии. Схема базы данных (PostgreSQL) уже содержит необходимые таблицы для большинства данных (`users`, `user_roadmap_progress`, `chat_messages`), однако фронтенд продолжает использовать `localStorage` для хранения критически важного пользовательского прогресса. Это создает риск потери данных при смене устройства или очистке браузера. Требуется рефакторинг фронтенда для полноценного использования существующих API.

**Статистика:** Обнаружено **10** типов данных в локальном хранилище. Из них **4 критических**, требующих немедленной миграции в БД.

---

## Таблица найденных проблем

| # | Данные | Где хранится | Файл/Компонент | Есть в БД? | Рекомендация | Приоритет |
|---|--------|--------------|----------------|------------|--------------|-----------|
| 1 | **vibes_roadmap_progress** | localStorage | `Roadmaps.tsx` | ✅ `user_roadmap_progress` | Переписать на API (GET/POST) | 🔴 Критично |
| 2 | **vibes_chat_history** | localStorage | `Assistant.tsx` | ✅ `chat_messages` | Загружать историю из БД | 🔴 Критично |
| 3 | **vibes_onboarded_{id}** | localStorage | `App.tsx` | ✅ `users.onboarding_completed` | Использовать поле из профиля | 🔴 Критично |
| 4 | **vibes_invites** | localStorage | `App.tsx` | ✅ `invite_links` | Удалить fallback, данные уже идут с API | 🟡 Важно |
| 5 | **vibes_students_db** | localStorage | `App.tsx` | ✅ `users` | **Удалить** (устаревший mock) | 🟡 Важно |
| 6 | **vibes_ai_system_instruction** | localStorage | `Assistant.tsx`, `AdminAssistant.tsx` | ✅ `ai_system_instructions` | Использовать API для получения активной | 🟡 Важно |
| 7 | **vibes_token** | localStorage | `fetchWithAuth.ts`, `App.tsx` | ❌ (JWT) | Оставить (стандартная практика) | 🟢 Норма |
| 8 | **theme** | localStorage | `App.tsx` | ❌ | Можно добавить в настройки профиля | 🟢 Низкий |
| 9 | **vibes_sound_enabled** | localStorage | `SoundContext.tsx` | ❌ | Оставить локально | 🟢 Низкий |
| 10| **vibes_sound_volume** | localStorage | `SoundContext.tsx` | ❌ | Оставить локально | 🟢 Низкий |

---

## Детальный анализ критических зон

### 1. Прогресс дорожных карт (`vibes_roadmap_progress`)
**Текущее состояние:** Данные о выполненных шагах пишутся в `localStorage` внутри `useEffect` в `Roadmaps.tsx`.
**Риски:** При заходе с телефона прогресс с десктопа будет недоступен.
**Решение:**
1. Использовать таблицу `user_roadmap_step_progress`.
2. В `Roadmaps.tsx` заменить чтение `localStorage` на вызов API `GET /api/progress/roadmaps`.
3. При клике на шаг отправлять `POST /api/progress/roadmaps/step`.

### 2. История чата (`vibes_chat_history`)
**Текущее состояние:** История сохраняется в массив в `localStorage`. При отправке сообщения *вся* история отправляется на бэкенд для контекста, но сам бэкенд её не сохраняет для будущего отображения (или фронтенд не запрашивает).
**Риски:** Потеря контекста обучения при очистке кэша.
**Решение:**
1. Использовать таблицу `chat_messages`.
2. Реализовать эндпоинт `GET /api/chat/history`.
3. При загрузке `Assistant.tsx` подтягивать последние 50 сообщений.

### 3. Статус онбординга (`vibes_onboarded_{userId}`)
**Текущее состояние:** В `App.tsx` проверяется ключ `vibes_onboarded_USERID`.
**Риски:** Пользователь видит онбординг каждый раз при смене устройства.
**Решение:**
1. Использовать поле `onboarding_completed` в таблице `users`.
2. Обновлять это поле через `PATCH /api/users/me` при завершении онбординга.
3. В `App.tsx` опираться на данные из `currentUser`.

---

## План миграции

### Этап 1: Ликвидация технического долга (1-2 дня)
1. Удалить код, связанный с `vibes_students_db` (mock-данные) из `App.tsx`.
2. Убрать дублирование логики инвайтов в `App.tsx` (оставить только API).

### Этап 2: Синхронизация прогресса (2-3 дня)
1. Реализовать API эндпоинты для чтения/записи прогресса roadmap.
2. Обновить `Roadmaps.tsx` для работы с сервером.
3. Реализовать миграцию: при первом входе, если есть локальные данные, но нет серверных — отправить локальные на сервер.

### Этап 3: Чат и Онбординг (2 дня)
1. Подключить сохранение сообщений в БД на бэкенде.
2. Реализовать загрузку истории чата.
3. Переключить проверку онбординга на поле профиля пользователя.

---

## Чек-лист проверки

- [x] Проверен весь фронтенд-код на использование Web Storage API
- [x] Проверены все cookies (не используются, только JWT в localStorage)
- [x] Изучена полная схема базы данных (таблицы существуют)
- [x] Проанализирован весь API (частично реализован, но не используется фронтендом)
- [x] Изучен функционал админ-панели (данные грузятся с API, но есть легаси-код)
- [x] Проверены все формы на сайте
- [x] Проверены пользовательские настройки
- [x] Проверена корзина/избранное (Roadmaps progress)
- [x] Проверена авторизация и сессии

**Оценка трудозатрат:** Около **30-40 часов** разработки для полного перехода на серверное хранение данных (Server-Side Storage).
